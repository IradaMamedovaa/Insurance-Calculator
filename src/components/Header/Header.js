import React from "react";
import "./style.scss"

const Header = () => {
  return (
    <div className="header">
      <header className="App-header">Insurance calculator</header>
    </div>
  );
};

export default Header;
